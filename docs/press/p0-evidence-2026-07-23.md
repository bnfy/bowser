# P0 release-safety evidence — July 23, 2026

## Source state

- Baseline commit: `f337e1bd7d8a4eafdb75db510c897e281504522b`
- Baseline matched `origin/main` when checked.
- P0 is checkpointed at commit `84cbd0a` on
  `codex/press-release-rc`. The subsequent vertical-tabs and press-package
  work is included in the release-candidate change set after passing its
  complete P1 gate.
- Package metadata is set to `1.0.0-rc.1`; no RC tag, draft, artifact set,
  or public release exists yet.

## Local gates passed

- Electron `43.2.0` is both installed and current in the npm registry check.
- Production dependency audit: zero vulnerabilities.
- All 241 unit tests, substrate checks, acceptance dry run, Astro site build,
  DNS smoke, deterministic desktop OAuth, and all 47 runnable desktop
  acceptance scenarios pass. F28 vertical tabs accounts for 12 live Electron
  scenarios and 116 steps inside that total.
- Apple Developer ID identity and embedded provisioning profile preflight
  pass.
- Apple notarization environment values resolve from 1Password without
  exposing their values.
- The signed unpacked Apple Silicon package passes the post-sign identity,
  provisioning-profile, and entitlement verifier.
- Packaged cold-online startup, pre-consent no-install-ID behavior,
  persisted opt-out choices, corrupt-cache/offline failure UI, and explicit
  Continue-without-blocking behavior pass outside acceptance mode.
- Public Stable v0.22.0 passes Developer ID, Gatekeeper, and stapled-ticket
  validation. It and the candidate then restore the same representative
  settings, grouped/pinned session, favorite, and history profile.
- Cloudflare authentication has Pages write access for the BNFY account.

## Fail-closed release controls

- Candidate/Stable mode, platform selection, and Mac architecture selection
  are explicit.
- Notarization failure, workflow dispatch/registration failure, incomplete
  Windows signing, unexpected Windows publisher, missing/stale assets,
  checksum mismatch, and dirty/non-main release inputs stop publication.
- Assets stage in one authenticated draft and are verified before the draft
  is published.
- GitHub Actions use the current published `actions/checkout@v6` and
  `actions/setup-node@v6` majors.

## External gates still open

- GitHub CLI authentication is restored for `bnfy`; repository secrets and
  variables were audited. Azure has tenant/client credentials plus the signing
  account and endpoint, but no certificate profile/publisher configuration, so
  Windows remains omitted rather than falling into an invalid signing attempt.
- The corrected workflow files are not on the default branch until the exact
  candidate source is reviewed, committed, and merged.
- No exact-version notarized RC exists yet, so there are no staged artifact
  hashes or clean-install results to record.
- Native Intel, Windows 11, and Linux x86_64 candidate tests remain absent;
  those targets stay out of the eligible matrix.
- The seven-day no-P0/P1 quiet period starts only after an immutable RC is
  distributed.
